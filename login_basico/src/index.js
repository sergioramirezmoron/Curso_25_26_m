// Importariamos el css de la app

import { initialApp } from "./app";

initialApp();

// Terminar el login mostrando en verde el login correcto y en rojo el login incorrecto
// Debajo hacer otro formulario para registrarse. EL formulario se debe llamar register.
// El registro debe permitir registrarse con un password hasheado y un username.
